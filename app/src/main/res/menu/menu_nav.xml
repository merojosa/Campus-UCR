<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/home_foro"
            android:icon="@drawable/ic_foro_home"
            android:title="Inicio"/>
        <item android:id="@+id/temas_foro"
            android:icon="@drawable/ic_foro_temas"
            android:title="Temas"/>
        <item
            android:id="@+id/mis_preguntas_foro"
            android:icon="@drawable/ic_mis_preguntas_foro"
            android:title="Mis preguntas">
        </item>
    </group>

    <item android:title="">
        <menu>
            <group android:checkableBehavior="single">
            <item android:id="@+id/pref_foro"
                android:icon="@drawable/ic_foro_pref"
                android:title="Preferencias" />

            <item android:id="@+id/logout_foro"
                android:icon="@drawable/ic_foro_logout"
                android:title="Cerrar Sesión" />
            </group>
        </menu>
    </item>
</menu>